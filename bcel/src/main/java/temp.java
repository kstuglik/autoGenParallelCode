public class temp {
    private static int resultRows;
    private static int resultColumns;
    
    public static void main(String[] args) {
        subTask(2, 100, 10);
    }

    private static void subTask(int rowNum, int colNum, int step) {
        for (int currRow = rowNum; currRow < rowNum + step && currRow < re; currRow++) {

        }
    }
}